from typing import List


def list_budgets_for_user(user_id: int) -> List[dict]:
    # placeholder returning mock budgets
    return [
        {"id": 1, "name": "Groceries", "target": 400.0, "progress": 120.0},
    ]
